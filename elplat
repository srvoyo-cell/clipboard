Диалог:
Пользователь: Добрый день, не могу войти в личный кабинет, появляется ошибка 401.
Техподдержка: Здравствуйте. Мы сбросили токен авторизации. Попробуйте выйти из приложения и войти заново.
Пользователь: Не помогло, ошибка 401 всё равно появляется.
Техподдержка: Поняли. Проверим дополнительно. Есть ли ещё проблемы?
Пользователь: Да, ещё при оплате заказа через СБП в мобильном приложении появляется ошибка SBP-502, деньги не списываются. Также не приходят push-уведомления о смене статуса заказа.
Техподдержка: Уточните версию приложения и банк для оплаты через СБП.
Пользователь: Версия приложения 6.14.2, Т-Банк. Ошибка SBP-502 повторяется. Push-уведомления тоже не приходят. А вход в личный кабинет по-прежнему не работает с ошибкой 401.

curl -sS -X POST 'http://127.0.0.1:100/' \
  -H 'Content-Type: application/json' \
  -d '{
    "action": "ask_llm",
    "pydantic_schema": "general_parse",
    "quantity": 1,
    "temperature": 0.1,
    "top_p": 0.9,
    "max_tokens": 160,
    "reasoning_effort": "none",
    "prompt": "Извлеки из диалога техподдержки все проблемы пользователя, которые остаются нерешёнными на конец диалога.\n\nПравила:\n- анализируй весь диалог;\n- исключай проблемы, которые пользователь подтвердил как решённые;\n- если проблема уточнялась, используй последнюю и самую полную формулировку;\n- одна проблема = один короткий элемент списка;\n- не добавляй предположения и действия поддержки;\n- не используй слова \"пользователь\", \"клиент\", \"сообщает\", \"жалуется\";\n- сохраняй важные термины, ошибки, коды, версии, сервисы и устройства;\n- формулировки должны подходить для поиска по базе знаний;\n- максимум 25 слов на проблему.\n\nЕсли нерешённых проблем нет, верни пустой список.",
    "text": "Пользователь: Добрый день, не могу войти в личный кабинет, появляется ошибка 401.\nТехподдержка: Здравствуйте. Мы сбросили токен авторизации. Попробуйте выйти из приложения и войти заново.\nПользователь: Не помогло, ошибка 401 всё равно появляется.\nТехподдержка: Поняли. Проверим дополнительно. Есть ли ещё проблемы?\nПользователь: Да, ещё при оплате заказа через СБП в мобильном приложении появляется ошибка SBP-502, деньги не списываются. Также не приходят push-уведомления о смене статуса заказа.\nТехподдержка: Уточните версию приложения и банк для оплаты через СБП.\nПользователь: Версия приложения 6.14.2, Т-Банк. Ошибка SBP-502 повторяется. Push-уведомления тоже не приходят. А вход в личный кабинет по-прежнему не работает с ошибкой 401."
  }'

===
Одна проблема решена, две актуальны
Диалог:
Пользователь: Добрый день, не проходит оплата банковской картой, пишет DECLINED-14.
Техподдержка: Попробуйте удалить карту и привязать её заново.
Пользователь: Удалил и привязал заново, карта теперь работает, оплата прошла.
Техподдержка: Отлично. Есть ли ещё вопросы?
Пользователь: Да, но чек на почту не пришёл. Ещё в приложении заказ остался в статусе "ожидает оплаты", хотя деньги уже списались.
Техподдержка: Уточните номер заказа и почту.
Пользователь: Заказ 84521, почта test@example.com. Чека всё ещё нет, статус заказа по-прежнему "ожидает оплаты".

curl -sS -X POST 'http://127.0.0.1:100/' \
  -H 'Content-Type: application/json' \
  -d '{
    "action": "ask_llm",
    "pydantic_schema": "general_parse",
    "quantity": 1,
    "temperature": 0.1,
    "top_p": 0.9,
    "max_tokens": 160,
    "reasoning_effort": "none",
    "prompt": "Извлеки из диалога техподдержки все проблемы пользователя, которые остаются нерешёнными на конец диалога.\n\nПравила:\n- анализируй весь диалог;\n- исключай проблемы, которые пользователь подтвердил как решённые;\n- если проблема уточнялась, используй последнюю и самую полную формулировку;\n- одна проблема = один короткий элемент списка;\n- не добавляй предположения и действия поддержки;\n- не используй слова \"пользователь\", \"клиент\", \"сообщает\", \"жалуется\";\n- сохраняй важные термины, ошибки, коды, версии, сервисы и устройства;\n- формулировки должны подходить для поиска по базе знаний;\n- максимум 25 слов на проблему.\n\nЕсли нерешённых проблем нет, верни пустой список.",
    "text": "Пользователь: Добрый день, не проходит оплата банковской картой, пишет DECLINED-14.\nТехподдержка: Попробуйте удалить карту и привязать её заново.\nПользователь: Удалил и привязал заново, карта теперь работает, оплата прошла.\nТехподдержка: Отлично. Есть ли ещё вопросы?\nПользователь: Да, но чек на почту не пришёл. Ещё в приложении заказ остался в статусе \"ожидает оплаты\", хотя деньги уже списались.\nТехподдержка: Уточните номер заказа и почту.\nПользователь: Заказ 84521, почта test@example.com. Чека всё ещё нет, статус заказа по-прежнему \"ожидает оплаты\"."
  }'

===
Доставка: старая проблема решена, новая актуальна
Диалог:
Пользователь: Здравствуйте, не могу изменить адрес доставки в заказе 91277.
Техподдержка: Мы открыли редактирование адреса. Попробуйте снова.
Пользователь: Да, адрес изменился, спасибо.
Техподдержка: Отлично, чем ещё можем помочь?
Пользователь: Теперь другая проблема: курьерская доставка пропала из вариантов, доступен только самовывоз.
Техподдержка: Проверьте, пожалуйста, город и индекс.
Пользователь: Москва, 125009. Курьерская доставка всё равно не отображается, только самовывоз.

curl -sS -X POST 'http://127.0.0.1:100/' \
  -H 'Content-Type: application/json' \
  -d '{
    "action": "ask_llm",
    "pydantic_schema": "general_parse",
    "quantity": 1,
    "temperature": 0.1,
    "top_p": 0.9,
    "max_tokens": 160,
    "reasoning_effort": "none",
    "prompt": "Извлеки из диалога техподдержки все проблемы пользователя, которые остаются нерешёнными на конец диалога.\n\nПравила:\n- анализируй весь диалог;\n- исключай проблемы, которые пользователь подтвердил как решённые;\n- если проблема уточнялась, используй последнюю и самую полную формулировку;\n- одна проблема = один короткий элемент списка;\n- не добавляй предположения и действия поддержки;\n- не используй слова \"пользователь\", \"клиент\", \"сообщает\", \"жалуется\";\n- сохраняй важные термины, ошибки, коды, версии, сервисы и устройства;\n- формулировки должны подходить для поиска по базе знаний;\n- максимум 25 слов на проблему.\n\nЕсли нерешённых проблем нет, верни пустой список.",
    "text": "Пользователь: Здравствуйте, не могу изменить адрес доставки в заказе 91277.\nТехподдержка: Мы открыли редактирование адреса. Попробуйте снова.\nПользователь: Да, адрес изменился, спасибо.\nТехподдержка: Отлично, чем ещё можем помочь?\nПользователь: Теперь другая проблема: курьерская доставка пропала из вариантов, доступен только самовывоз.\nТехподдержка: Проверьте, пожалуйста, город и индекс.\nПользователь: Москва, 125009. Курьерская доставка всё равно не отображается, только самовывоз."
  }'

===
Мобильное приложение: несколько актуальных технических проблем
Диалог:
Пользователь: После обновления приложение зависает на заставке.
Техподдержка: Очистите кэш приложения и перезапустите телефон.
Пользователь: После очистки кэша приложение стало открываться.
Техподдержка: Хорошо. Остались ещё ошибки?
Пользователь: Да. В профиле не сохраняется новый номер телефона, появляется ошибка VALIDATION_PHONE. Ещё при открытии раздела "Бонусы" приложение вылетает.
Техподдержка: Уточните устройство, ОС и версию приложения.
Пользователь: iPhone 13, iOS 17.5, приложение 4.8.1. Номер не сохраняется, ошибка VALIDATION_PHONE остаётся. Раздел "Бонусы" всё ещё крашит приложение.

curl -sS -X POST 'http://127.0.0.1:100/' \
  -H 'Content-Type: application/json' \
  -d '{
    "action": "ask_llm",
    "pydantic_schema": "general_parse",
    "quantity": 1,
    "temperature": 0.1,
    "top_p": 0.9,
    "max_tokens": 200,
    "reasoning_effort": "none",
    "prompt": "Извлеки из диалога техподдержки все проблемы пользователя, которые остаются нерешёнными на конец диалога.\n\nПравила:\n- анализируй весь диалог;\n- исключай проблемы, которые пользователь подтвердил как решённые;\n- если проблема уточнялась, используй последнюю и самую полную формулировку;\n- одна проблема = один короткий элемент списка;\n- не добавляй предположения и действия поддержки;\n- не используй слова \"пользователь\", \"клиент\", \"сообщает\", \"жалуется\";\n- сохраняй важные термины, ошибки, коды, версии, сервисы и устройства;\n- формулировки должны подходить для поиска по базе знаний;\n- максимум 25 слов на проблему.\n\nЕсли нерешённых проблем нет, верни пустой список.",
    "text": "Пользователь: После обновления приложение зависает на заставке.\nТехподдержка: Очистите кэш приложения и перезапустите телефон.\nПользователь: После очистки кэша приложение стало открываться.\nТехподдержка: Хорошо. Остались ещё ошибки?\nПользователь: Да. В профиле не сохраняется новый номер телефона, появляется ошибка VALIDATION_PHONE. Ещё при открытии раздела \"Бонусы\" приложение вылетает.\nТехподдержка: Уточните устройство, ОС и версию приложения.\nПользователь: iPhone 13, iOS 17.5, приложение 4.8.1. Номер не сохраняется, ошибка VALIDATION_PHONE остаётся. Раздел \"Бонусы\" всё ещё крашит приложение."
  }'



from __future__ import annotations

import argparse
from pathlib import Path

import cv2
import matplotlib.pyplot as plt
import numpy as np
from PIL import Image, ExifTags


def read_image(image_path: str | Path) -> tuple[np.ndarray, np.ndarray]:
    image_path = Path(image_path)

    bgr = cv2.imread(str(image_path))
    if bgr is None:
        raise ValueError(f"Cannot read image: {image_path}")

    rgb = cv2.cvtColor(bgr, cv2.COLOR_BGR2RGB)
    gray = cv2.cvtColor(bgr, cv2.COLOR_BGR2GRAY)

    return rgb, gray


def has_camera_exif(image_path: str | Path) -> bool:
    image = Image.open(image_path)
    exif = image.getexif()

    if not exif:
        return False

    tag_names = {
        ExifTags.TAGS.get(k, k): v
        for k, v in exif.items()
    }

    camera_tags = {
        "Make",
        "Model",
        "LensModel",
        "DateTimeOriginal",
        "ExposureTime",
        "FNumber",
        "ISOSpeedRatings",
        "FocalLength",
    }

    return any(tag in tag_names for tag in camera_tags)


def normalize_uint8(image: np.ndarray) -> np.ndarray:
    image = image.astype(np.float32)
    image = image - image.min()
    image = image / (image.max() + 1e-8)
    return (image * 255).astype(np.uint8)


def blur_score(gray: np.ndarray) -> float:
    """
    Меньше = более мутно.
    Фото монитора часто мутнее настоящего скриншота.
    """
    return float(cv2.Laplacian(gray, cv2.CV_64F).var())


def noise_score(gray: np.ndarray) -> float:
    """
    Больше = больше мелкого шума.
    Фото камерой обычно шумнее скриншота.
    """
    gray_f = gray.astype(np.float32)
    smooth = cv2.GaussianBlur(gray_f, (5, 5), 0)
    noise = gray_f - smooth
    return float(np.std(noise))


def glare_mask(rgb: np.ndarray) -> np.ndarray:
    """
    Маска бликов/засветов.
    """
    hsv = cv2.cvtColor(rgb, cv2.COLOR_RGB2HSV)
    h, s, v = cv2.split(hsv)

    bright = v > 245
    low_saturation = s < 40

    mask = bright & low_saturation
    return mask.astype(np.uint8) * 255


def glare_score(rgb: np.ndarray) -> float:
    mask = glare_mask(rgb)
    return float((mask > 0).mean())


def edge_map(gray: np.ndarray) -> np.ndarray:
    return cv2.Canny(gray, 80, 160)


def high_frequency_map(gray: np.ndarray) -> np.ndarray:
    """
    Карта высокочастотных деталей.
    На фото монитора тут часто проявляются сетки, муар, полосы.
    """
    gray_f = gray.astype(np.float32)
    blurred = cv2.GaussianBlur(gray_f, (21, 21), 0)
    high_freq = gray_f - blurred
    return normalize_uint8(np.abs(high_freq))


def moire_score(gray: np.ndarray) -> float:
    """
    Примерная оценка регулярных высокочастотных паттернов через FFT.
    Больше = больше подозрение на муар/сетку.
    """
    gray_f = gray.astype(np.float32)

    blurred = cv2.GaussianBlur(gray_f, (21, 21), 0)
    high_freq = gray_f - blurred

    spectrum = np.fft.fft2(high_freq)
    spectrum = np.fft.fftshift(spectrum)
    magnitude = np.log1p(np.abs(spectrum))

    h, w = magnitude.shape
    cy, cx = h // 2, w // 2

    y, x = np.ogrid[:h, :w]
    radius = min(h, w) // 10

    center_mask = (x - cx) ** 2 + (y - cy) ** 2 <= radius ** 2
    outer_mask = ~center_mask

    high_freq_energy = magnitude[outer_mask].mean()
    total_energy = magnitude.mean() + 1e-8

    return float(high_freq_energy / total_energy)


def straight_lines_score(gray: np.ndarray) -> float:
    """
    Количество длинных прямых линий.
    У фото экрана часто есть рамки монитора, границы окна, перспективные края.
    """
    edges = edge_map(gray)

    lines = cv2.HoughLinesP(
        edges,
        rho=1,
        theta=np.pi / 180,
        threshold=120,
        minLineLength=min(gray.shape[:2]) // 4,
        maxLineGap=20,
    )

    if lines is None:
        return 0.0

    return float(len(lines))


def sigmoid(x: float) -> float:
    return 1 / (1 + np.exp(-x))


def probability_photo_of_monitor(
    *,
    has_exif: bool,
    blur: float,
    noise: float,
    glare: float,
    moire: float,
    lines: float,
) -> float:
    """
    Эвристическая вероятность.
    Это не обученная модель, а scoring-функция для первичной фильтрации.

    На своём датасете пороги лучше подкрутить.
    """

    score = 0.0

    # EXIF камеры — очень сильный сигнал.
    if has_exif:
        score += 2.5

    # Фото монитора часто менее резкое.
    if blur < 80:
        score += 1.3
    elif blur < 150:
        score += 0.9
    elif blur < 300:
        score += 0.4
    else:
        score -= 0.6

    # Фото камерой часто шумнее.
    if noise > 12:
        score += 1.1
    elif noise > 8:
        score += 0.7
    elif noise > 5:
        score += 0.3
    else:
        score -= 0.4

    # Блики/засветы.
    if glare > 0.03:
        score += 1.0
    elif glare > 0.01:
        score += 0.5

    # Муар / регулярные высокие частоты.
    if moire > 1.12:
        score += 1.0
    elif moire > 1.08:
        score += 0.5

    # Длинные линии.
    if lines > 40:
        score += 0.6
    elif lines > 20:
        score += 0.3

    # Сдвигаем, чтобы обычные картинки не слишком часто попадали в photo_monitor.
    score -= 1.2

    return float(sigmoid(score) * 100)


def analyze_image(image_path: str | Path) -> dict:
    image_path = Path(image_path)

    rgb, gray = read_image(image_path)

    exif = has_camera_exif(image_path)
    blur = blur_score(gray)
    noise = noise_score(gray)
    glare = glare_score(rgb)
    moire = moire_score(gray)
    lines = straight_lines_score(gray)

    probability = probability_photo_of_monitor(
        has_exif=exif,
        blur=blur,
        noise=noise,
        glare=glare,
        moire=moire,
        lines=lines,
    )

    if probability >= 70:
        label = "photo_of_monitor"
    elif probability <= 30:
        label = "not_photo_of_monitor"
    else:
        label = "uncertain"

    return {
        "image_path": str(image_path),
        "label": label,
        "probability": probability,
        "has_camera_exif": exif,
        "blur_score": blur,
        "noise_score": noise,
        "glare_score": glare,
        "moire_score": moire,
        "straight_lines_score": lines,
        "rgb": rgb,
        "gray": gray,
        "edges": edge_map(gray),
        "glare_mask": glare_mask(rgb),
        "high_frequency_map": high_frequency_map(gray),
    }


def show_result(result: dict) -> None:
    probability = result["probability"]
    label = result["label"]

    title = (
        f"{label} | probability: {probability:.1f}%\n"
        f"EXIF: {result['has_camera_exif']} | "
        f"blur: {result['blur_score']:.1f} | "
        f"noise: {result['noise_score']:.1f} | "
        f"glare: {result['glare_score']:.4f} | "
        f"moire: {result['moire_score']:.3f} | "
        f"lines: {result['straight_lines_score']:.0f}"
    )

    fig, axes = plt.subplots(2, 3, figsize=(15, 9))
    fig.suptitle(title, fontsize=13)

    axes[0, 0].imshow(result["rgb"])
    axes[0, 0].set_title("Original")

    axes[0, 1].imshow(result["gray"], cmap="gray")
    axes[0, 1].set_title("Grayscale")

    axes[0, 2].imshow(result["edges"], cmap="gray")
    axes[0, 2].set_title("Edges")

    axes[1, 0].imshow(result["glare_mask"], cmap="gray")
    axes[1, 0].set_title("Glare mask")

    axes[1, 1].imshow(result["high_frequency_map"], cmap="gray")
    axes[1, 1].set_title("High-frequency map")

    # Визуальная шкала вероятности
    prob_bar = np.zeros((80, 400), dtype=np.uint8)
    filled = int(probability / 100 * prob_bar.shape[1])
    prob_bar[:, :filled] = 255

    axes[1, 2].imshow(prob_bar, cmap="gray", vmin=0, vmax=255)
    axes[1, 2].set_title(f"Photo monitor probability: {probability:.1f}%")

    for ax in axes.ravel():
        ax.axis("off")

    plt.tight_layout()
    plt.show()


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("image_path", type=str, help="Path to image file")
    args = parser.parse_args()

    result = analyze_image(args.image_path)

    print(f"file: {result['image_path']}")
    print(f"label: {result['label']}")
    print(f"photo_of_monitor_probability: {result['probability']:.2f}%")
    print(f"has_camera_exif: {result['has_camera_exif']}")
    print(f"blur_score: {result['blur_score']:.2f}")
    print(f"noise_score: {result['noise_score']:.2f}")
    print(f"glare_score: {result['glare_score']:.6f}")
    print(f"moire_score: {result['moire_score']:.4f}")
    print(f"straight_lines_score: {result['straight_lines_score']:.0f}")

    show_result(result)


if __name__ == "__main__":
    main()
